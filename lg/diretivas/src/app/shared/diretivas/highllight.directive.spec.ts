import { HighllightDirective } from './highllight.directive';

describe('HighllightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighllightDirective();
    expect(directive).toBeTruthy();
  });
});
