import { FiltroArrayInpuroPipe } from './filtro-array-inpuro.pipe';

describe('FiltroArrayInpuroPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroArrayInpuroPipe();
    expect(pipe).toBeTruthy();
  });
});
